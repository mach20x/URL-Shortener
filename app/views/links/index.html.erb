<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Original</th>
      <th>Shortened URL</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  <% @links.each do |url| %>
      <tr>
        <td><%= link.id %></td>
        <td><%= link.longURL %></td>
        <td><%= link.shortURL %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>Visit https://peaceful-basin-98810.herokuapp.com/(id) to be redirected.
</p>
